<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
       http://dubbo.apache.org/schema/dubbo http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- provider's application name, used for tracing dependency relationship -->
    <dubbo:application name="demo-provider"/>
    <!-- use multicast registry center to export service -->
    <dubbo:registry address="multicast://224.5.6.7:1234"/>
    <!-- use dubbo protocol to export service on port 20880 -->
    <dubbo:protocol name="dubbo" port="20885"/>
    <!-- service implementation, as same as regular local bean -->
    <bean id="rpcPoint" class="edu.nuaa.o4raft.interfacee.impl.RaftPointService"/>
    <!-- declare the service interface to be exported -->
    <dubbo:service group="e" interface="edu.nuaa.o4raft.interfacee.RaftPoint" ref="rpcPoint"/>


    <dubbo:reference id="otherPoint" group="a,b,c,d" merger="true"  check="false" interface="edu.nuaa.o4raft.interfacee.RaftPoint">
        <dubbo:method name="requestVote" merger="vote" />
        <dubbo:method name="appendEntries" merger="append" />
    </dubbo:reference>
</beans>